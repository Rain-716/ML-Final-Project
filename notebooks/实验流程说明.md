# 实验流程说明
1. 运行 `python src/train.py --data_source sample` 做离线演示。
2. 运行 `python src/train.py --data_source empathetic --max_samples 8000` 使用成熟公开数据集。
3. 查看 `outputs/metrics.csv`、混淆矩阵、学习曲线与特征重要性。
4. 运行 `python src/app.py` 进行答辩演示。
